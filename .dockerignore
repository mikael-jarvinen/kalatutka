# Both Dockerfiles share this build context. Each only COPYs:
#   web      → src/, deploy/nginx.conf
#   refresh  → scripts/, deploy/refresh-entrypoint.sh
# Everything else is dead weight in the image and can be excluded.

# Version control
.git/
.gitignore
.gitattributes

# Docker / compose (no need inside the image)
Dockerfile*
docker-compose*.yml
.dockerignore

# Project docs and meta — not needed at runtime
README.md
CLAUDE.md
docs/

# Node tooling — not used here, but defensive
node_modules/
npm-debug.log*
yarn-debug.log*
package-lock.json

# Editor
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Local-only outputs
/tmp/
*.log

# The refresh container is the sole writer of src/data/. Don't bake the
# committed snapshot into the web image — it just causes volume-init
# permission mismatches with the non-root refresh user.
src/data/

# Environment files (never bake secrets into images)
.env
.env.*
